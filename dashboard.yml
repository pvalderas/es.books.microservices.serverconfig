# HTTP Server
server:
  port: 5555   # HTTP (Tomcat) port
  
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:2222/eureka

turbine:
  aggregator:
    clusterConfig: ejemplo-hystrix
  appConfig: ejemplo-hystrix
  amqp:
    port: 8989
