# Discovery Server Access
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:2222/eureka

# HTTP Server
server:
  port: ${PORT:3333}   # HTTP (Tomcat) port
  
security:
  basic:
    enabled: false
